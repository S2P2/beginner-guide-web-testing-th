
# Module 0: พื้นฐาน

## 📝 แผนการเรียน

โมดูลนี้ออกแบบให้คุณทำตามความเร็วของตัวเองได้ ทำตามขั้นตอนเหล่านี้เพื่อสร้างความรู้พื้นฐาน

| ขั้นตอน | เป้าหมายและกิจกรรมของคุณ | เวลาโดยประมาณ |
| :--- | :--- | :--- |
| **1. เริ่มต้น** | อ่านวัตถุประสงค์ของโมดูล สักครู่หนึ่ง ให้คิดว่าคุณอยากจะบรรลุอะไรกับทักษะใหม่นี้ | 2 นาที |
| **2. ทำความเข้าใจโครงสร้างของเว็บไซต์** | เรียนรู้ส่วนประกอบหลักสามส่วนของเว็บไซต์โดยเปรียบเทียบกับบ้าน เป้าหมายคือ ทำความเข้าใจบทบาทที่แตกต่างกันของ HTML, CSS และ JavaScript | 5 นาที |
| **3. เรียนรู้โค้ดพื้นฐาน** | ศึกษาแนวคิดการเขียนโปรแกรม 4 หัวข้อที่จำเป็น: Variables, Strings, Functions และ Async/Await พยายามทำความเข้าใจจุดประสงค์ของตัวอย่างโค้ดแต่ละตัว | 5 นาที |
| **4. การประเมินความรู้** | ทบทวนแนวคิดหลักของโมดูลนี้ คุณสามารถอธิบาย พื้นฐานของ HTML, CSS, JavaScript และแนวคิดการเขียนโปรแกรมสี่ประการได้ด้วยคำพูดของคุณเองหรือไม่ จดบันทึกประเด็นต่างๆ ที่คุณไม่แน่ใจ เพื่อกลับมาศึกษาต่อ | 5 นาที |

## 🎯 วัตถุประสงค์

วัตถุประสงค์ของคุณในโมดูลนี้คือ การสร้างความเข้าใจพื้นฐานเกี่ยวกับวิธีการทำงานของเว็บไซต์ และการเข้าใจแนวคิดการเขียนโปรแกรมที่จำเป็นสำหรับการทดสอบอัตโนมัติ เราจะตรวจสอบว่าแม้ว่าคุณจะเป็นมือใหม่ที่สุด คุณก็จะสร้างแบบจำลองจิตใจที่ชัดเจนก่อนที่คุณจะเริ่มเขียนโค้ดใดๆ

## 📖 เนื้อหา

### เว็บไซต์คืออะไร? การเปรียบเทียบกับบ้าน

ก่อนที่เราจะทดสอบเว็บไซต์ได้ เราต้องรู้ว่าเว็บไซต์ประกอบด้วยอะไร ลองนึกถึงเว็บไซต์เหมือนกับบ้าน:

<figure markdown="span">
    ![HTML-CSS-JS](https://www.keentodesign.com.au/cdn-cgi/imagedelivery/eOylWWvDYZyJkbAUtQZpuQ/www.keentodesign.com.au/html.png/w=768)
    <figcaption>แหล่งที่มา: https://www.keentodesign.com.au/difference-between-css-html-and-javascript/</figcaption>
</figure>

*   **HTML (HyperText Markup Language) คือโครงสร้าง** เป็นพื้น กำแพง หลังคา และประตู ทำให้บ้านมีรูปร่าง หากไม่มีโครงสร้างเหล่านี้ ก็จะเป็นที่ว่างเปล่า สำหรับเว็บไซต์ HTML ใช้เป็นพิมพ์เขียวสร้างปุ่ม แบบฟอร์ม และกล่องข้อความต่างๆ
    ```html
    <!-- เช่นถ้าปุ่มคือ "ประตู" -->
    <button id="submitButton">Click Me</button>
    ```

*   **CSS (Cascading Style Sheets) คือสไตล์** คือการทาสีผนัง กำหนดรูปแบบเฟอร์นิเจอร์ และการตกแต่งต่างๆ มทำให้บ้านดูดีขึ้น น่าใช้ สำหรับเว็บไซต์ CSS ควบคุมสี ตัวอักษร และเค้าโครงตำแหน่งขององค์ประกอบต่างๆ ของเว็บ
    ```css
    /* โค้ดนี้ทำให้ "ประตู" ของเรากลายเป็นสีน้ำเงิน */
    #submitButton {
      background-color: #007bff;
      color: white;
    }
    ```

*   **JavaScript (JS) คือฟังก์ชันการทำงาน** มันคือไฟฟ้า ท่อน้ำ และตัวเปิดประตูโรงแรม มันทำให้บ้านสามารถ*ทำงาน*ได้ เมื่อคุณสลับสวิตช์ไฟจะเปิด บนเว็บไซต์ JavaScript จัดการสิ่งที่เกิดขึ้นเมื่อคุณคลิกปุ่ม ส่งแบบฟอร์ม หรือโต้ตอบกับหน้าเว็บ

หน้าที่ของเราในการทดสอบอัตโนมัติคือการเป็น "ผู้ตรวจสอบบ้าน" เราไม่จำเป็นต้องเป็นสถาปนิก (HTML) ผู้ออกแบบภายใน (CSS) หรือช่างไฟฟ้า (JS) แต่เรา*ต้อง*รู้วิธีการเปิดประตู สลับสวิตช์ และตรวจสอบว่าทุกอย่างทำงานตามที่คาดหวังหรือไม่

### ฉันต้องใช้การเขียนโปรแกรมแบบไหน? ขั้นต่ำที่จำเป็น

คุณไม่จำเป็นต้องเป็นนักพัฒนาโปรแกรมมืออาชีพเพื่อเขียนการทดสอบ! คุณเพียงแต่ต้องเข้าใจแนวคิดหลักเพียงบางอย่างเท่านั้น ลองนึกถึงพวกเขาเป็นชุดเครื่องมือพื้นฐานของคุณ

1.  **Variables: กล่องติดป้าย**
    ตัวแปร (Variable) คือเพียงแค่คอนเทนเนอร์ที่มีป้ายกำกับซึ่งบรรจุสิ่งใดสิ่งหนึ่ง เราใช้มันเพื่อจัดเก็บข้อมูลที่เราต้องการใช้ต่อมา
    ```javascript
    // 'userName' คือป้ายกำกับบนกล่องที่บรรจุข้อความ "John Doe"
    let userName = "John Doe";
    ```

2.  **Strings: ข้อความ**
    "String" คือชื่ออย่างเป็นทางการของข้อความ เราเขียนปิดหัวท้ายด้วยเครื่องหมายคำพูด `"` (double quote)
    ```javascript
    let greeting = "Hello, World!";
    ```

3.  **Functions: ชุดคำสั่งที่ใช้ซ้ำได้**
    ฟังก์ชัน (Function) คือชุดคำสั่งที่มีชื่อซึ่งคุณสามารถเรียกใช้ได้ตลอดเวลา ซึ่งจะช่วยให้คุณไม่ต้องเขียนโค้ดเดิมๆ ซ้ำแล้วซ้ำอีก
    ```javascript
    // ชุดคำสั่งสำหรับการทักทายบุคคล
    function greet(name) {
      console.log("Hello, " + name);
    }

    // ใช้คำสั่งการทักทายซ้ำ!
    greet("Alice"); // พิมพ์ "Hello, Alice"
    greet("Bob");   // พิมพ์ "Hello, Bob"
    ```

4.  **Async/Await: คำสำคัญ "รอคิว"**
    นี่คือแนวคิดที่ยากที่สุด แต่สำคัญที่สุด เว็บไซต์ไม่ได้โหลดทันที เมื่อคุณคลิกปุ่ม คุณอาจต้องรอให้บางสิ่งบางอย่างเกิดขึ้น `async` และ `await` คือคำสำคัญพิเศษที่บอกให้สคริปต์ทดสอบของเราทราบว่า: "เฮ้ ขั้นตอนต่อไปนี้อาจใช้เวลาสักครู่ **รอ**ให้มันจบสิ้นก่อนที่คุณจะดำเนินการต่อ"

    ```javascript
    // 'async' บอก JavaScript ให้คาดหวังการรอบางอย่างในฟังก์ชันนี้
    async function submitForm() {
      // 'await' หยุดที่นี่จนกว่าปุ่มจะถูกคลิกและหน้าเว็บจะตอบสนอง
      await page.click("#submitButton");

      // บรรทัดนี้จะไม่รันจนกว่าการคลิกจะเสร็จสิ้น
      console.log("Button has been clicked!");
    }
    ```
    เกือบทุกการทดสอบที่คุณเขียนจะใช้ `async/await` (บาง framework อาจใส่มาให้โดยเราไม่ต้องเขียนเอง)


### ➡️ ต่อไป: การสร้างบ้านจริงๆ — HTML & DOM

ตอนนี้ที่คุณเข้าใจบล็อกการสร้างพื้นฐานของเว็บไซต์แล้ว ถึงเวลาที่จะ**ดูพิมพ์เขียวบ้านอย่างใกล้ชิด**
ในบทเรียนถัดไป เราจะมาดูตัวอย่าง HTML จริง สำรวจวิธีการที่เบราว์เซอร์เปลี่ยนมันให้เป็นโครงสร้างที่มีชีวิตเรียกว่า **DOM** และเรียนรู้ลงมือตรวจบ้าน: **Inspecting Elements (ตรวจสอบองค์ประกอบหน้าเว็บ)**
